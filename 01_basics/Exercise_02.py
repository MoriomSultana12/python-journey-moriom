#Write a Python program that:
#Takes the user’s name as input
#Takes the user’s favorite color as input
#Prints a sentence Moriom likes pink

# Taking user's name as input and storing it in a variable called 'name'
name = input('Whats your name ?')
# Taking user's favorite color as input and storing it in a variable called 'color'
color = input ("whats your fav color?")
# Printing the result by combining (concatenating) name and color
print(name+'likes'+color)
